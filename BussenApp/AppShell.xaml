<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="BussenApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:BussenApp"
    xmlns:pages="clr-namespace:BussenApp.Pages"
        xmlns:roundPages="clr-namespace:BussenApp.Pages.Rounds"

    Shell.FlyoutBehavior="Disabled"
    Title="BussenApp">

    <ShellContent
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <ShellContent
                ContentTemplate="{DataTemplate local:MainPage}"
                Route="MainPage" />

    <ShellContent
                ContentTemplate="{DataTemplate pages:PlayersPage}"
                Route="Game" />

    <ShellContent
                ContentTemplate="{DataTemplate roundPages:CardsPage}"
                Route="Cards" />



</Shell>
